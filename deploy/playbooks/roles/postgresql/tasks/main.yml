---

- include: install.yml

- name: ensure database service is up
  service:
    name: postgresql
    state: started
  sudo: yes

- include: users.yml

- include: configure.yml

- name: make {{ app_name }} database
  postgresql_db:
    name: "{{ app_name }}"
    owner: "{{ app_name }}"
    state: present
    login_user: postgres
  sudo_user: postgres
  sudo: yes
